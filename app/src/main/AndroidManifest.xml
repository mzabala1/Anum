<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anum.sites.google.com.site.anum20172">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme1">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NonLinearMethods" />
        <activity android:name=".SolutionsOfSystemsOfEquations" />
        <activity android:name=".InterpolationMethods"></activity>
        <activity android:name=".Grapher"></activity>
        <activity android:name=".IncSearch"></activity>
        <activity android:name=".FixedPoint"></activity>
    </application>

</manifest>